{# 四阶段选题生成 Prompt 模板 — 1003.01 #}
{% if phase == "extract" %}
你是一个知识点提取专家。从以下搜索结果中提取与主题相关的核心知识点。

主题: {{ topic }}

搜索结果摘要:
{{ snippets }}

请提取出有研究价值的知识点，每个知识点包含名称和描述（至少10字）。

输出 JSON 格式:
{
  "knowledge_points": [
    {"knowledge_point": "知识点名称", "description": "详细描述（至少10字）"}
  ]
}

要求:
- 提取 3~8 个知识点
- 每个知识点应具有独立的研究价值
- 描述应准确概括该知识点的核心内容
{% elif phase == "loose_filter" %}
你是一个选题质量评估专家。请对以下知识点进行初步筛选，移除明显不适合深入研究的内容。

知识点列表:
{{ points_json }}

筛选标准（宽松）:
- 移除过于基础、常识性的内容（如 "1+1=2"）
- 移除过于宽泛、无法聚焦的主题
- 保留有研究深度和实用价值的知识点

输出 JSON 格式:
{
  "filtered_points": [
    {"knowledge_point": "...", "description": "..."}
  ]
}
{% elif phase == "explore" %}
你是一个创意发散专家。请为以下知识点生成多个有价值的研究/写作方向。

知识点: {{ knowledge_point }}
描述: {{ description }}

请生成 5~10 个具体的研究方向或文章选题，每个方向应:
- 具有明确的切入角度
- 有足够的深度可以展开
- 对目标读者有实际价值

输出 JSON 格式:
{
  "research_ideas": ["方向1", "方向2", "方向3", ...]
}
{% elif phase == "strict_filter" %}
你是一个严格的选题评审专家。请对以下研究方向进行严格筛选。

知识点: {{ knowledge_point }}

候选方向:
{{ ideas_json }}

严格筛选标准:
- 保留最有深度和价值的方向
- 至少保留 1 个方向
- 至少淘汰 2 个方向（如果候选数 >= 3）
- 给出淘汰原因

输出 JSON 格式:
{
  "kept_ideas": ["保留的方向1", "保留的方向2"],
  "rejected_ideas": ["淘汰的方向1", ...],
  "reasons": {"淘汰的方向1": "淘汰原因", ...}
}
{% elif phase == "statement" %}
你是一个技术写作顾问。请基于以下选题方向，生成一份结构化的选题陈述文档。

主题: {{ topic }}

精选的选题方向:
{{ ideas_json }}

请用 Markdown 格式输出选题陈述，包含:
1. 主题概述
2. 每个选题方向的简要说明和预期价值
3. 推荐的写作优先级

直接输出 Markdown 内容，不要包裹在代码块中。
{% endif %}
